# DTO snapshot input

Place or link Zhengtao's `gathered_snapshots_hex.hdf5` here, or pass another path with:

```bash
python 02_compute_dto_fourier.py --snapshots /path/to/gathered_snapshots_hex.hdf5
```
